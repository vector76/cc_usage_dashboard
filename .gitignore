.env
.env.*
!.env.example
.raymond/
/.bm

# Build artifacts (anchored to repo root so they don't clobber source dirs)
/cli
/clusage-cli
/trayapp
/trayapp.exe
*.o
*.a
*.so
*.dylib

# IDE
.vscode/
.idea/
*.swp
*.swo
*~

# OS
.DS_Store
.DS_Store?
._*
.Spotlight-V100
.Trashes
ehthumbs.db
Thumbs.db

# Development
# config.yaml is materialized from config.sample.yaml on first run and is
# user-owned; only the sample is tracked.
/config.yaml
usage.db
usage.db-wal
usage.db-shm
*.log

# Screenshots dropped into the repo while iterating
Screenshot*.png
*.screenshot.png
